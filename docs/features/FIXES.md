the name "לוח בקרה" needs to be changed to "Homepage" / "דף הבית" 

number font need to be changed 

missingAmountPrompt and UpcomingEventsCard should be the same and in the homepage (no need for a special page for Google Calender) - stick with UpcomingEventsCard.

Categories / Envelopes: 

    When editing a category - the amount should be automatically marked for user's change.
    three points in envelopes (for deleting) card show up at the top of the page.
    put the editing as an option inside the "three points" - aside with deleting.
    Option to see the spending details when clicking on a specific category.
    adding validation for categories - category's name is distinct and cannot be duplicated 

settings - profile menu stuck at the top left side and doesnt disappear

Onboarding:
    appears to existing users (need to be fixed)
    refactoing to a nice, user-friendly onboarding page.
    
"תכנון הוצאות / צפוי" (in dashboard) shows only "הוצאות בפועל" without the budget (amount of all categories budget)

in planned-expenses page: 
    upcoming events card is unnecesarry.

